server:
  port: 0 # 0?? ??, ????? ????? ??

spring:
  application:
    name: user-service-${server.port}

eureka:
  instance:
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}} # ????? ???? ??? ??, ????? ????? ?? 1?? ??? ??? ???? ?? ???? ?? ??
  client:
    register-with-eureka: true
    fetch-registry: true # ??? ????? ?????? ??? ????? ??? ???? ???? ??. true? ??, ?? ? ??? ???? ??
    service-url:
      defaultZone: http://localhost:8761/eureka
